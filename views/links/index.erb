<h1> Welcome <%= @name %>!</h1>

<h1> Links</h1>
<ul id='links'>
  <% @links.each do |link| %>
  <li>
    Title: <%=link.title %>
      URL: <%= link.url %>
      tags: <%=link.tags.map(&:name)%>
    </li>
    <% end %>
  </ul>

<form action='/links/new'>
<button type='submit' name='New Link'>New Link</button>
</form>
